import './Price.css'

const Price = (props) => {
    return (
        <>
            {/* <div className="burger_prices">
                {props.ingredients.map((ingredient) => <p key={ingredient.name}>{ingredient.name}: <span>{ingredient.price} $</span></p>)}
            </div> */}
        </>
    )
}
export default Price